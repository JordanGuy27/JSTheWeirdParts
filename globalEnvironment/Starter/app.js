//base execution context is the global context
//creates a global object and 'this'

//when the page is run JavaScript creates this global context

//Global = not inside a function


var a = 'hello world';

function b() {
    
}